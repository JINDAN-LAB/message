<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd	
	http://www.springframework.org/schema/beans 
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
    http://code.alibabatech.com/schema/dubbo 
    http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
	
	<dubbo:application name="message-provider" />
    <dubbo:registry address="${message-registry-address}" protocol="zookeeper"/>
    <dubbo:protocol name="dubbo" port="${message-service-port}" accesslog="true" />
    
	<dubbo:service ref="userService" group="${message-service-group}" version="${message-service-version}" timeout="${message-service-timeout}"
				   interface="com.jd.message.facade.UserService" />
	<dubbo:service ref="customerService" group="${message-service-group}" version="${message-service-version}" timeout="${message-service-timeout}"
				   interface="com.jd.message.facade.CustomerService" />
	<dubbo:service ref="projectService" group="${message-service-group}" version="${message-service-version}" timeout="${message-service-timeout}"
				   interface="com.jd.message.facade.ProjectService" />

</beans>